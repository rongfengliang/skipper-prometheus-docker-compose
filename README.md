# skipper  && prometheus

## how to run

* start

```code
docker-compose up -d
```

* prometheus/metrics
  
```code
open http://localhost:9911/metrics
```

* access demo website(nginx)

```code
open http://localhost:9090
```